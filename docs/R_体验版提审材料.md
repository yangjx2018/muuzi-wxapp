# R.3 · 体验版提审材料（仅准备）

> **禁止**：未经负责人明确授权，不得上传体验版 / 提交审核 / 发布正式版。  
> 本文只整理材料清单与草稿口径，便于授权后一次性填表。

**整理日**：2026-09-22  
**工程 AppId（project.config.json）**：`wxf40b35f27b303bbe`（若与公众平台正式号不一致，以上传账号为准）

---

## 1. 基本信息草稿

| 项 | 建议填写 |
|----|----------|
| 小程序名称 | MuuZi（以公众平台已注册名为准） |
| 简介 | 主理人连接、主页与消息工具；登录所选 GuDuu OS 节点，与 MuuZi App 同账号体系 |
| 服务类目 | 优先「工具 · 信息查询」或「社交 · 社区/论坛」（须与实际能力一致；最终以类目审核口径为准） |
| 标签 | 连接、名片、消息、创作者 |

---

## 2. 隐私与用户知情

| 材料 | 状态 / 要点 |
|------|-------------|
| 用户隐私保护指引（公众平台） | **待运营填写正式链接**；须声明：账号密码与 Matrix 会话仅发往用户所选节点；微信 `code` 仅发往当前节点换票；不向 Nexus 发送登录凭据 |
| 登录页法律说明 | 已落地：`pages/auth/login`「只会送往你选择的 GuDuu OS 节点，不会送往 Nexus」 |
| 语音采集告知 | 已落地：FieldTalk「跨语种翻译与语音识别由 Google Cloud 处理；录音处理后清理…」 |
| 收集的信息 | 节点账号、微信登录 code（换票）、主页/店铺等 Creator 资料、现场交流文本/语音（经同意）、Matrix 消息 |
| 权限 | 麦克风（现场语音）；相册/相机（会话发图）；可选订阅消息（通知设置） |

---

## 3. 服务器域名

见 `docs/P0_微信合法域名清单.md`。

| 类型 | 主机（已于 2026-09-21 保存） |
|------|------------------------------|
| request / upload / download | `im.muuzi.co` · `www.muuzi.co` · `dev-os.guduu.co` · `guduuos.com` |
| socket | `im.muuzi.co` · `dev-os.guduu.co` · `guduuos.com` |
| web-view 业务域名 | 本期不用，留空 |

上传体验版前：确认 `urlCheck: true`；删除本机 `miniprogram/config.local.js`（若有）。

---

## 4. 测试账号与路径说明（给审核员）

| 项 | 内容 |
|----|------|
| 测试节点 | 白名单内可登录节点（如 `im.muuzi.co`）；勿写仅 develop 本地 bot |
| 密码测试号 | （负责人提供，勿写入 Git） |
| 微信测试 | 未绑定：弹框验证绑定；已绑定：一键进连接 |
| 主路径 | 登录 → 连接 Hub →（可选）名片/话题 → 消息列表与文本会话 → 我 / 设置 / 退出 |
| 已知不可测 | 完整 E2EE 密钥恢复；加密房附件发送；未配置时的微信 JSAPI 支付（页内诚实提示） |

---

## 5. 上传前自检（授权后执行）

- [ ] 正式/体验环境无 `config.local.js`、无本地 bot 进白名单  
- [ ] `project.config.json` AppId 与上传账号一致  
- [ ] 服务器域名与清单一致；`node check.js` 与全量测试绿  
- [x] 体验版/正式版打开 `pages/dev/*` 会拦截并回登录（`utils/devGuard.js`）；仍勿在审核说明里写 dev 路径  
- [ ] 隐私指引链接已上线且与登录文案一致  
- [ ] 负责人书面授权「上传体验版」后再操作  

---

## 6. 包体

源码树 `miniprogram/` 约 **0.6 MB** 量级（无大媒体资源），低于微信主包 2 MB 限制；上传前仍以开发者工具「代码质量 / 包大小」面板为准。
