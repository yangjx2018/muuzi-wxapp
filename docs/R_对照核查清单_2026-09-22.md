# 小程序 ↔ App 对照核查清单

**核查日**：2026-09-22  
**主仓**：`MuuziWx`（对照真值 `MuuziGit/muuzi`）  
**范围**：登录 · 连接 · 消息 · 我（三 Tab；不做首页/Muu Tab）  
**方法**：源码对照 + `node check.js` / `node --test`（129 pass）；真机联调另栏。

---

## 0. 结论摘要

| 模块 | 主路径可用性 | 与 App 一致性 | 最严重缺口 |
|------|--------------|---------------|------------|
| 登录 M0 | 工程可用（公网微信待配） | 高（**F** 齿轮/显隐）；微信替代 Apple | **K-11** 公网契约 A |
| 连接 M1 | Talk 写 Matrix / Host / Continue / Join 可用 | 高～中（**D**）；待真机双端 | 真机扫码↔Join 签字 |
| 消息 M3 | 明文收发可用 | 中高（**B+C+G**） | 真机视觉签字；E2EE 故意延后 |
| 我 M2 | 主页/设置/子页主路径可用 | 主页高（**E**）；子页减配列表壳豁免 | **K-12** JSAPI；Muu stub |

**代码批次**：B–G **已完成**。下一步：`R_批次真机验收清单_2026-09-22.md` + 运维 A。

---

## 1. Blocker（主路径不可用 / 与 App 关键行为断裂）

| ID | 模块 | 问题 | App 证据 | 小程序证据 | 状态 |
|----|------|------|----------|------------|------|
| B-01 | 连接/Talk | 确认发言**未写入** Matrix 现场房间；仅清本机 outbox | `matrix-field.ts` `save` + `FieldTalk.saveTurn` → `saveFieldRecord` | `talk/index.js` `persistConfirmed` 只 `outbox.clear`；无 `matrix-field` 服务 | **已修**（`matrixField.js` + Talk 接线） |
| B-02 | 连接/扫码 | **宿主扫码交流**整条链缺失（prepare/create/decide/revoke/chat） | `FieldHostInvite.tsx` + `host/*` | Talk 扫码态仅跳转访客 Join；`fieldNodeApi` 无 host 路由 | **已修**（`pages/connect/host` + host 路由） |

---

## 2. Critical（重要主路径错误）

| ID | 模块 | 问题 | 状态 |
|----|------|------|------|
| C-01 | 连接/继续 | Continue 不能恢复宿主邀请/QR；`canResume` 恒 false | **已修** |
| C-02 | 连接/Talk | 未 `openFieldTopic` 即开始录制；App 需 topicAccess | **已修**（随 B-01） |
| C-03 | 消息 | `@我` 筛选依赖 `room.mentioned`，列表投影从未赋值 → 恒空 | **已修**（`highlight`→`mentioned`） |
| C-04 | 消息 | 发起私信**总是 createRoom**，不复用已有 DM | **已修** |
| C-05 | 消息/E2EE | 加密房禁发、密文不可解密（产品 K-02）；若强求对等则为 Critical | 记录 / 本期诚实延后 |

---

## 3. Major（可用但明显不对齐）

| ID | 模块 | 问题 | 状态 |
|----|------|------|------|
| M-01 | 名片 | 「去编辑」仅 toast+切 Tab，不进对应编辑页 | **已修** |
| M-02 | 已保存现场 | 过滤用 `room.field`，App 用 field space `channelIds` | **已修**（优先 channelIds，回退 field） |
| M-03 | Join | 无 `watchClosure` 跨实例关闭同步 | **已修**（本机 cleared 标记 + onShow/动作前 check） |
| M-04 | 消息 | 接受邀请不打开会话、不注册 `m.direct` | **已修** |
| M-05 | 消息 | `followedPeers` 未接线 →「关注私聊」空 | **已修** |
| M-06 | 消息 | 无入站「已读」气泡 UI | **已修**（sync receipts） |
| M-07 | 消息 | 无房间设置面板 | **已修** |
| M-08 | 消息 | Tab 未读不计待处理邀请 | **已修** |
| M-09 | 消息 | 收件箱无 nodeWorkspaces 抽屉项 | **已修** |
| M-10 | 我 | agent/sharing/enterprise/saved 占位 | **已修**（Muu 仍占位） |
| M-11 | 我 | 主体深度偏薄 | **已修**（作品库/动态/认证/短链/代理与分享入口） |
| M-12 | 我 | 店铺无商品目录/外链工具 | **已修**（精选/发布/实物外链；编辑主页短链+IG/TikTok 展示） |
| M-13 | 我 | 留言箱无节点 inbox bind/unbind UI | **已修** |
| M-14 | Hub | `onHide` 重复定义 | **已修** |

---

## 4. Minor / Intentional

| ID | 说明 |
|----|------|
| I-01 | 三 Tab vs 五 Tab；Muu 中心 Tab 不做（PRD） |
| I-02 | 微信登录 vs Apple/Google；Studio 复制链接 vs 打开；JSAPI vs IAP |
| I-03 | 语音 RecorderManager vs FieldAudioBridge；振幅/最短时长已补（K-10） |
| I-04 | 赠卡双方均为 stub |
| I-05 | Join 多粘贴入口（小程序深链限制，可接受） |
| I-06 | 无独立 Discover/Files Tab；发布已在「我」/空间/主体接通；供给可贴中央商城；表情分类已对齐 App |

---

## 5. 文档自相矛盾（已处理）

| 项 | 说明 |
|----|------|
| K-13 / 排期 M1.4 | 曾写「保存主路径已通」但代码未调 Matrix save；已按 B-01 接通并改文档 |

---

## 6. 整改顺序（执行）

1. **B-01 + C-02**：移植 `matrix-field`（HTTP）并接入 Talk 保存/开题 — **已完成**  
2. **C-03 / C-04 / M-04 / M-05 / M-08**：消息收件箱与私信/邀请 — **已完成**  
3. **M-10**：接通代理 / Agent卡 / 企业邀请 / 收藏 — **已完成**  
4. **B-02 + C-01**：宿主扫码 + Continue 恢复 — **已完成**  
5. **M-01…M-14 / K-17…K-19**：店铺、主体、发布（含「我」入口与商城挂品）、表情等 — **Major 代码侧已完成**  
6. 复查：单测 + 关键路径走查；更新本清单与 `R_已知问题.md` — **持续**

---

## 7. 验证记录

| 检查 | 结果 | 时间 |
|------|------|------|
| 源码对照审计 | 完成；见 §1–§5 | 2026-09-22 |
| `node check.js` | ok | 2026-09-22 |
| `node --test test/*.test.js` | **129 pass**（B–G 后） | 2026-09-22 |
| 真机双端互通 | **待签**：见 `R_批次真机验收清单_2026-09-22.md` | — |
